# AGENTS.md

## 开发原则

- 以当前需求和当前最佳设计为最高依据；当开发者提出的需求不明确或不符合最佳实践时，需主动与开发者沟通并完善。
- 先了解相关行为、代码和文档，并参考成熟产品采用的模式；将项目历史视为证据，而不是必须延续的约束。
- 选择完全满足当前需求的最简单实现，避免推测性的抽象、配置和间接层。
- 在不损害目标设计的前提下，优先采用小步、端到端的方式交付变更；如果增量修改会保留错误结构或累积兼容层，则进行大范围重构或整体重写。
- 明确分离职责；只有在能够降低耦合或复杂性时，才引入新的模块或层级。
- 检查项目现有依赖的文档和类型定义后，优先复用其已有能力。只有在能够明显提高可靠性或降低总体复杂性时，才考虑引入维护良好的新库。
- 兼容性不是默认约束。以当前需求和当前最佳设计为准，即使需要大范围重构或整体重写。仅保护任务中明确指定的外部契约和不可恢复资产；直接删除过时的内部实现，不添加兼容层、回退逻辑或并行路径。
- 优先采用持久方案，而不是计划日后替换的权宜实现；同时不要为假设性的未来需求进行设计。

## 工作规范

- 仅修改当前任务所需内容，并保留用户已有的无关改动。
- 创建、更新或执行 `docs/dev/` 中的工作文档前，先阅读 `docs/dev/README.md`，并遵循其中的文档、授权、进度和验收规范。
- 大范围重构或整体重写前，明确当前需求、需要保护的契约与资产，以及验收标准。
- 安装依赖、执行破坏性操作、提交或推送前，必须获得明确授权。
- 修改代码后运行 `npm test`。仅修改文档时，检查文档内容和差异。完成时说明修改内容、验证结果和遗留风险。
